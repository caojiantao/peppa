<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace用作标识mapper的唯一，通常包名+文件名 -->
<mapper namespace="com.cjt.dao.security.IUserDAO">

    <select id="getDatas" resultType="com.cjt.entity.model.security.User" parameterType="com.cjt.entity.dto.UserDTO">
        SELECT
        id,
        username,
        password,
        nickname
        FROM user
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id!=null">AND id = #{id}</if>
            <if test="username!=null and username!=''">AND `username` = #{username}</if>
            <if test="password!=null">AND `password` = #{password}</if>
        </trim>
        <if test="start!=null and offset!=null">LIMIT #{start}, #{offset}</if>
    </select>

    <select id="getDatasTotal" resultType="int" parameterType="com.cjt.entity.dto.UserDTO">
        SELECT COUNT(*)
        FROM user
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id!=null">AND id = #{id}</if>
            <if test="username!=null">AND `username` = #{username}</if>
            <if test="password!=null">AND `password` = #{password}</if>
        </trim>
    </select>

    <!-- 想要获取最后一次插入的主键id，注意mybatis和ibatis的区别 -->
    <insert id="insert" parameterType="com.cjt.entity.model.security.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname)
        VALUES (#{username}, #{password}, #{nickname})
    </insert>

    <delete id="deleteById" parameterType="int">
        DELETE FROM user
        WHERE id = #{id}
    </delete>

    <update id="updateById" parameterType="com.cjt.entity.model.security.User">
        UPDATE user
        <trim prefix="SET" suffix="WHERE id = #{id}" suffixOverrides=",">
            <if test="username!=null and username!=''">`username` = #{username},</if>
            <if test="password!=null and password!=''">`password` = #{password},</if>
            <if test="nickname!=null and nickname!=''">`nickname` = #{nickname},</if>
        </trim>
    </update>

    <select id="login" resultType="com.cjt.entity.model.security.User">
        SELECT
            id,
            username,
            password,
            nickname
        FROM user
        WHERE
            `username` = #{username}
            AND `password` = #{password}
    </select>
</mapper>