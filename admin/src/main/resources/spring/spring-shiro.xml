<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
            http://www.springframework.org/schema/util
            http://www.springframework.org/schema/util/spring-util.xsd">

    <!--  Cookie 管理器  -->
    <bean id="token" class="org.apache.shiro.web.servlet.SimpleCookie">
        <constructor-arg value="${token_name}"/>
        <property name="httpOnly" value="true"/>
        <property name="maxAge" value="${token_maxAge}"/>
    </bean>
    <bean id="rememberMeManager" class="com.cjt.admin.shiro.CookieManager">
        <!--<property name="cipherKey" value="#{T(org.apache.shiro.codec.Base64).decode('4AvVhmFLUs0KTA3Kprsdag==')}"/>-->
        <property name="cookie" ref="token"/>
    </bean>

    <!-- 用户登录权限控制 -->
    <bean id="jdbcRealm" class="org.apache.shiro.realm.jdbc.JdbcRealm">
        <property name="dataSource" ref="dataSource"/>
        <property name="cachingEnabled" value="false"/>
    </bean>

    <!-- 无状态会话 -->
    <bean id="subjectFatory" class="com.cjt.admin.shiro.SubjectFactory"/>
    <bean id="sessionManager" class="org.apache.shiro.session.mgt.DefaultSessionManager">
        <property name="sessionValidationSchedulerEnabled" value="false"/>
    </bean>
    <bean id="sessionStorageEvaluator" class="org.apache.shiro.web.mgt.DefaultWebSessionStorageEvaluator">
        <property name="sessionStorageEnabled" value="false"/>
    </bean>
    <bean id="subjectDAO" class="org.apache.shiro.mgt.DefaultSubjectDAO">
        <property name="sessionStorageEvaluator" ref="sessionStorageEvaluator"/>
    </bean>

    <!-- Shiro 管理器 -->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="subjectFactory" ref="subjectFatory"/>
        <property name="sessionManager" ref="sessionManager"/>
        <property name="subjectDAO" ref="subjectDAO"/>
        <property name="realm" ref="jdbcRealm"/>
        <property name="rememberMeManager" ref="rememberMeManager"/>
    </bean>

    <!-- 相当于调用SecurityUtils.setSecurityManager(securityManager) -->
    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="staticMethod" value="org.apache.shiro.SecurityUtils.setSecurityManager"/>
        <property name="arguments" ref="securityManager"/>
    </bean>

    <bean id="tokenFilter" class="com.cjt.admin.shiro.TokenFilter"/>

    <!-- Shiro的Web过滤器 -->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager"/>
        <property name="loginUrl" value="/demo/login"/>
        <!-- 没有权限跳转页面 -->
        <property name="unauthorizedUrl" value="/unauthorized"/>
        <property name="filters">
            <util:map>
                <entry key="token" value-ref="tokenFilter"/>
            </util:map>
        </property>
        <property name="filterChainDefinitions">
            <value>
                / = anon
                /static/** = anon
                /manager/** = anon
                /demo/** = anon
                /session/** = anon
                /** = token
            </value>
        </property>
    </bean>

    <!-- 保证实现了Initializable接口的shiro object能够在spring自动调用init或者destroy方法，母鸡原理，用就对了 -->
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>
</beans>